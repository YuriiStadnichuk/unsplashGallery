import reducer from './reducer';

import * as offersActions from './actions';
import * as offersTypes from './types';
import * as offersSelectors from './selectors';

export {offersActions, offersTypes, offersSelectors};

export default reducer;
