import reducer from './reducer';

import * as homeActions from './actions';
import * as homeTypes from './types';
import * as homeSelectors from './selectors';

export {homeActions, homeTypes, homeSelectors};

export default reducer;
